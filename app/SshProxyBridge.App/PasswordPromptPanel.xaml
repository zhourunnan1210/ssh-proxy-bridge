<UserControl x:Class="SshProxyBridge.App.PasswordPromptPanel"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Width="480" Height="350"
        Background="{StaticResource WindowBackgroundBrush}"
        FontFamily="Microsoft YaHei UI, Microsoft YaHei">
    <Grid Margin="28">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Text="需要服务器密码" FontSize="21" FontWeight="SemiBold"
                   Foreground="{StaticResource TextPrimaryBrush}" />
        <TextBlock x:Name="DescriptionText" Grid.Row="1" Margin="0,8,0,18"
                   TextWrapping="Wrap" Foreground="{StaticResource TextSecondaryBrush}" />

        <PasswordBox x:Name="PasswordBox" Grid.Row="2" Height="42" Padding="11,8"
                     BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" />
        <StackPanel Grid.Row="3" Margin="0,13,0,0">
            <TextBlock x:Name="ErrorText" Visibility="Collapsed"
                       Foreground="#B91C1C" Margin="0,0,0,10" />
            <CheckBox x:Name="SavePasswordCheckBox"
                      Content="成功后保存到 Windows 凭据管理器" />
            <TextBlock Text="密码只用于本次公钥安装，不会进入日志、Profile 或命令行。"
                       Foreground="{StaticResource TextSecondaryBrush}" FontSize="12"
                       Margin="0,10,0,0" TextWrapping="Wrap" />
        </StackPanel>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="取消" Style="{StaticResource SecondaryButtonStyle}"
                    Click="Cancel_Click" Margin="0,0,10,0" />
            <Button Content="继续" Style="{StaticResource PrimaryButtonStyle}"
                    Click="Continue_Click" IsDefault="True" />
        </StackPanel>
    </Grid>
</UserControl>
